# Public Convex URL — shown in the Convex dashboard after first deploy
# Prefix PUBLIC_ is required for SvelteKit to expose this to the browser
PUBLIC_CONVEX_URL=https://<your-deployment>.convex.cloud
